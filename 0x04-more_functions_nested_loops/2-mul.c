#include "main.h"

/**
 * mul - multiplies 2 integers.
 * @a: the first number to multiply.
 * @b: the second number to multiply.
 * Return: The result of the multplication between a and b.
 */

int mul(int a, int b)
{
	return (a * b);
}
