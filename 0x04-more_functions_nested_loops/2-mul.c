#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integeres
 * @a: the first number to multiply
 * @b: the second number to multiply
 * Return: The result of the multipliation between a param and b param
 */

int mul(int a, int b)
{
	return (x * y);
}
